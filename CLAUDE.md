# PeerProof —— 接手这个项目之前先读这里

参加者押金报名,到场后互相扫码作证,合约自动结算:确认到场的人拿回押金,并瓜分没来的人
被没收的押金。**主办方没有任何一个函数能放行、扣留或收到一分钱** —— 这是整个产品的立论,
任何让主办方碰到钱的改动都在推翻它。

为 Monad Metropolis 黑客松做的,截止 **2026-10-13**。
线上:<https://zhxinyue29.github.io/peerproof/>

---

## 先读哪几份

| 文件 | 什么时候读 |
|---|---|
| `docs/换台电脑开工.md` | 环境怎么搭、怎么上线、四条别碰的线 |
| `docs/设计稿/README.md` | **动手改界面之前必读**,含"第零条"和三条硬规矩 |
| `docs/待办.md` | 做到哪了、每个页面的六条验收要求、用户定过的边界 |
| `docs/状态.md` | 现在线上是什么样、合约地址、已验证过什么 |
| `contracts/src/EventDirectory.sol.LOCKED` | 碰合约之前 |

---

## 四条红线(改坏了不可逆)

1. **`contracts/src/EventDirectory.sol` 一个字符都不能改,注释也不行。**
   它的地址是 CREATE2 从字节码算出来的,字节码含源码哈希。改一个注释 → 地址变 →
   链上已有的所有活动标题、简介、标签全部读不到。
   已经犯过一次:改了个常量,把用户刚建的活动标题弄丢了,靠 `git show` 逐字节还原才救回来。

2. **`web/lib/wallet.ts` 里那几句英文不能翻译、不能改标点。**
   那段文字是拿去签名的,签名结果派生用户的密钥。字变了 = 派生出另一个账号 = 老用户
   的身份永久找不回来。

3. **Privy App Secret 绝不进仓库、绝不外传。** 代码里那个 `cmtxucm...` 是
   **App ID**,公开的,本来就要打进前端包;真正拦滥用的是 Privy 后台的域名白名单。

4. **gas limit 绝不手配公式,一律 `estimateGas` × 1.2。**
   Monad **按 gas limit 收费**,所以估低不是慢一点,是交易失败还照样全额扣钱。
   手配公式在这个项目栽过三次(部署/存资料/存描述),每次都是公式在猜 storage layout,
   结构体一改就失效而没人记得重推。兜底的 `offlineDescribeGas` 只在 RPC 抽风时用。

---

## 改界面的规矩

**设计稿在 `docs/设计稿/`,动手前把图打开** —— 别凭印象,印象会朝"我已经做出来的样子"漂。

- **先定版式,再填内容。** 第一轮只对:几栏、导航在哪、标题什么样。版式没确认别填块。
- **说"按稿子做完了"之前,先出一张并排对比图**(稿子 | 截图,拼一起)。
  只拿页面和它自己比(溢出、死按钮、点击目标)永远发现不了版式错——那些全过,
  页面照样和稿子毫无相似之处。
- **改动的理由必须在稿子里真找得到。** 稿子里没画的东西,别把猜测写成"稿子这么画的";
  这种地方先保持原样,最后列出来问用户。
- **截图自己验收,别让用户替你刷新页面。**

### 稿子上画了、但这个产品**不做**的(用户拍板过,别再"补全")

社区声誉值 / 活动满意度 / Verified Builder 徽章 / 主办方奖金池 / 我的身份 /
顶栏导航项 / 「1 分钟了解 PeerProof」播放链接。

理由是同一条:**这套系统产生不出这些数,或者和"主办方碰不到钱"冲突。**
画一个不起作用的开关,是这个产品能犯的最不诚实的错误。稿子的"验证方式"画了四种方法
(钱包/签到/定位/互证),合约只有互证一种 —— 所以做成了同一种方法的四条**真属性**,
版式照抄,话是真的。

---

## 怎么验证(这个项目吃过的亏)

- **绿勾 ≠ 页面对。** Actions 全绿、站点能开,展示的却是一场早就结束的活动 ——
  因为有人在 GitHub 后台设了个变量把它钉死,而那个变量在代码里、日志里、页面上都看不见。
  **推完必须去线上真刷一次。**
- **退出码 0 ≠ 干了活。** `scripts/gh-ssh-tunnel.py` 是 ProxyCommand,直接把 `git push`
  当参数传给它会静默成功、一个字节没发。推完看 `git status -sb` 有没有"领先 N"。
- **`?dev=1` 能自动化跑完整条链上流程。** 配合本机 anvil(`scripts/dev-chain.sh`)
  可以给测试账号打款,所以登录态的页面也能自动截图。线上也能用,只是那个地址没有 MON。
- `scripts/demo/seed-event.mjs` 一键建一场内容填满的活动(封面/标题/简介/地点/标签)。

---

## 代码惯例

- 注释写**为什么**,尤其写"这里为什么不是显然的那种写法" —— 现有注释大量记录了
  踩过的坑,照着这个密度写,别写 `// 设置标题` 这种。
- 中英双语,字典在 `web/lib/i18n`,类型强制同步,`scripts/i18n-audit.mjs` 查缺失。
  **新增文案必须两种语言都加**,不能硬编码中文。
- 静态导出(`output: export`),没有服务端。任何需要后端的方案都走不通。
- 改完至少跑:`npx tsc --noEmit` 和 `npm run build`。
