# 待办

**开工第一件事:读这个文件。做完一项把 `[ ]` 改成 `[x]` 并写上提交号。**

这里只写用户交代的事,不写我自己加的。要停之前先把这个文件更新到最新 —— 之前
「还剩什么」只活在聊天记录里,一停就丢,用户得重讲一遍。

---

## 任务

- [x] **1. 视频放到主页右上角原来那张图的位置**
      - [x] 放上去了 — `674e8c1`
      - [x] **验证用户看到的是动态的** —— 线上实测在播(1.72→2.92),这台机器没开
            「减少动画」。剩余嫌疑是浏览器缓存或窗口宽度 < 768px(那个尺寸只给静图)
      - [x] **验证动态顺畅** —— 3.75s / 24fps 播满,循环接缝交叉淡化 14 帧
      - [x] **稍微往左挪一点点** —— 已挪。视频左边缘到标语右端的间距 281px → **133px**。
            做法不是单纯拉宽:标语在 y≈335 右侧是空的,但下面两张入口卡伸到 x≈745,
            拉宽会压到卡上。改成**左侧渐隐走 107° 斜角** —— 上面进得来,下面躲开卡片。
      - [x] **字卡位置** —— 按可见带重排(右移 17%、缩到 0.56/0.52),3.42s 时整句完整可读。
            两次误判记在这里:①「seek 后 currentTime 一直是 0」是因为 Python 的
            http.server 不支持 Range 请求,本地没法 seek,不是视频的问题;
            ②「右边被切掉」是因为截图抓在 3.21s,揭开动画才走到 82%,右边 18% 本来
            就还没揭开 —— 把「正在揭开」看成了「被切掉」
- [x] **2. 视频位置微调** — 以用户视角看哪个位置最舒服,和其他模块的位置要协调
- [x] **3. 按截图做活动方的界面** — `ac67174`
      - [x] 问候横幅、四个数字、活动卡片(带填充条)、两步创建流程,都在
      - [x] 创建时补上「地点」——主办方卡片一直在标题下面印地点,而创建流程从来不收
      - [x] 简介编辑器补上「标签」;活动页搜索把地点和标签一起搜进去
      - [x] **修:describe 少传一个参数**,所有简介写入在发出去之前就失败(见 `ac67174`)
      - [x] 「先在左边选一场活动」代替永远转不完的骨架屏;「还没创建活动」和
            「正在加载活动」不再同时挂着
      - [x] 问候语按时间分早/下午/晚上(原来任何时候都说「早上好」)
- [x] **4. 按截图做个人资料的界面** — `6d975e2`
      - [x] 资料能填、能存、能读回来:名字、简介、城市、X、GitHub、网站
      - [x] **在本机 anvil 上真的跑通一遍**:登录 → 部署目录合约 → 填 → 存 → 回概览,
            名字取代地址出现在横幅上
      - [x] **修:部署按钮只有浏览器插件用户能按**(邮箱/passkey 路径给的是一句英文报错)
      - [x] **修:部署 gas 写死 1,400,000,实际要 1,689,094**——按一次就是花钱买一次失败
      - [x] **修:setProfile 的 gas 公式漏了「每个字段一个长度槽」**,填满资料必存不上
      - [x] `/me` 签出状态没有登录入口(却让人去别的页面登录)
- [ ] **5. 手机版界面同样要做** —— 排在自检和测试之前:现在手机界面像是没检查过一样,
      先做出来再谈检查
- [ ] **6. 扫码的界面做好** — `/floor`
- [ ] **7. 展示自己二维码的界面做好** — `/venue`
- [ ] **8. 自检:页面的样子是不是和设计图一样**
- [ ] **9. 自检:从用户角度看,每个模块是不是都在合适的位置**
- [ ] **10. 测试每一个按键是否都能用**
- [ ] **11. 全方位可用性测试**
- [ ] **12. 部署合约** — 要用户签名;或者直接建第一个活动,创建流程会自动先部署

**不要随便停止。** 清单没清空就继续下一项,不要做完一件就回来汇报。

**卡住就跳过,不要停。** 某一步实在搞不定,在这里写明卡在哪、试过什么,然后做下一项。

---

## 每个页面的验收要求(六条,缺一不算做完)

做完 3、4、9、10、11 里的任何一个页面,都要走完这六条再打勾:

- **a. 对着设计稿逐块核** —— 有的做了;没做的写明为什么不做
- **b. 从用户视角看每个模块的位置** —— 不是"有没有",是"在这儿舒不舒服"
- **c. 点每一个按钮** —— 确认都能用,不是看着能用
- **d. 手机版同样过一遍**
- **e. 截图存档**
- **f. 推上线,并核实线上真的生效** —— 不是推完就算,要去线上抓一次

---

## 用户定过的约定(不是任务,是边界)

- 社区声誉值、活动满意度 —— **不做**,这个系统永远不会产生这两个数
- 主办方奖金池 —— **不做**,与「主办方从头到尾碰不到一分钱」冲突
- 主办方押金(取消则扣) —— 做成**功能说明**,不写进合约
- USD 折算、较上月 —— **留着**,将来会有;现在显示 `—` 而不是 0
- 我的身份 —— 用户说去掉
- 活动标签、个人资料 —— **存链上**,不存浏览器本地(只有自己看得见等于没有)

## 我做不了、需要用户的

- 登录态的所有页面:无头浏览器没有会话,`/me`、`/organizer` 登录后的样子我截不到
- 三台真机扫码测试
- 任何要签名的链上写入
