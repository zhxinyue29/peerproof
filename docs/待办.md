# 待办

**开工第一件事:读这个文件。做完一项把 `[ ]` 改成 `[x]` 并写上提交号。**

这里只写用户交代的事,不写我自己加的。要停之前先把这个文件更新到最新 —— 之前
「还剩什么」只活在聊天记录里,一停就丢,用户得重讲一遍。

---

## 任务

- [ ] **1. 视频放到主页右上角原来那张图的位置**
      - [x] 放上去了 — `674e8c1`
      - [ ] **要验证用户打开网页看到的是动态的** —— 不是代码里有就算。用户反馈看到的
            仍是静态。已排除:线上视频确实在播(时间轴 1.72→2.92)、这台机器没开
            「减少动画」。仍需查:浏览器缓存、窗口宽度小于 768px 时只给静图
      - [ ] **验证动态是否顺畅**
      - [x] **稍微往左挪一点点** —— 已挪。视频左边缘到标语右端的间距 281px → **133px**。
            做法不是单纯拉宽:标语在 y≈335 右侧是空的,但下面两张入口卡伸到 x≈745,
            拉宽会压到卡上。改成**左侧渐隐走 107° 斜角** —— 上面进得来,下面躲开卡片。
      - [ ] **字卡被斜向渐隐切掉左边** —— 新问题。字卡是烧在视频里的,视频左边一淡,
            "Real Participation," 的开头就糊了。已把字卡在画面里右移 9%、缩到 0.80/0.70
            并重新导出,但**没验证成功**:浏览器可能缓存了旧的 webm,seek 到 3.45s 时
            字卡没出现。下一步:强制刷新后重测,或给 webm 加版本参数
      - [ ] 做不了就上网找办法或部署工具 —— 别人网站能做,就一定有方法
- [ ] **2. 视频位置微调** — 以用户视角看哪个位置最舒服,和其他模块的位置要协调
- [ ] **3. 按截图做活动方的界面**
- [ ] **4. 按截图做个人资料的界面**
- [ ] **5. 手机版界面同样要做** —— 排在自检和测试之前:现在手机界面像是没检查过一样,
      先做出来再谈检查
- [ ] **6. 扫码的界面做好** — `/floor`
- [ ] **7. 展示自己二维码的界面做好** — `/venue`
- [ ] **8. 自检:页面的样子是不是和设计图一样**
- [ ] **9. 自检:从用户角度看,每个模块是不是都在合适的位置**
- [ ] **10. 测试每一个按键是否都能用**
- [ ] **11. 全方位可用性测试**
- [ ] **12. 部署合约** — 要用户签名;或者直接建第一个活动,创建流程会自动先部署

**不要随便停止。** 清单没清空就继续下一项,不要做完一件就回来汇报。

**卡住就跳过,不要停。** 某一步实在搞不定,在这里写明卡在哪、试过什么,然后做下一项。

---

## 每个页面的验收要求(六条,缺一不算做完)

做完 3、4、9、10、11 里的任何一个页面,都要走完这六条再打勾:

- **a. 对着设计稿逐块核** —— 有的做了;没做的写明为什么不做
- **b. 从用户视角看每个模块的位置** —— 不是"有没有",是"在这儿舒不舒服"
- **c. 点每一个按钮** —— 确认都能用,不是看着能用
- **d. 手机版同样过一遍**
- **e. 截图存档**
- **f. 推上线,并核实线上真的生效** —— 不是推完就算,要去线上抓一次

---

## 用户定过的约定(不是任务,是边界)

- 社区声誉值、活动满意度 —— **不做**,这个系统永远不会产生这两个数
- 主办方奖金池 —— **不做**,与「主办方从头到尾碰不到一分钱」冲突
- 主办方押金(取消则扣) —— 做成**功能说明**,不写进合约
- USD 折算、较上月 —— **留着**,将来会有;现在显示 `—` 而不是 0
- 我的身份 —— 用户说去掉
- 活动标签、个人资料 —— **存链上**,不存浏览器本地(只有自己看得见等于没有)

## 我做不了、需要用户的

- 登录态的所有页面:无头浏览器没有会话,`/me`、`/organizer` 登录后的样子我截不到
- 三台真机扫码测试
- 任何要签名的链上写入
