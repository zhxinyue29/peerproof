# 待办

**开工第一件事:读这个文件。做完一项把 `[ ]` 改成 `[x]` 并写上提交号。**

这里只写用户交代的事,不写我自己加的。要停之前先把这个文件更新到最新 —— 之前
「还剩什么」只活在聊天记录里,一停就丢,用户得重讲一遍。

---

## 任务

- [x] **1. 视频放到主页右上角原来那张图的位置**
      - [x] 放上去了 — `674e8c1`
      - [x] **验证用户看到的是动态的** —— 线上实测在播(1.72→2.92),这台机器没开
            「减少动画」。剩余嫌疑是浏览器缓存或窗口宽度 < 768px(那个尺寸只给静图)
      - [x] **验证动态顺畅** —— 3.75s / 24fps 播满,循环接缝交叉淡化 14 帧
      - [x] **稍微往左挪一点点** —— 已挪。视频左边缘到标语右端的间距 281px → **133px**。
            做法不是单纯拉宽:标语在 y≈335 右侧是空的,但下面两张入口卡伸到 x≈745,
            拉宽会压到卡上。改成**左侧渐隐走 107° 斜角** —— 上面进得来,下面躲开卡片。
      - [x] **字卡位置** —— 按可见带重排(右移 17%、缩到 0.56/0.52),3.42s 时整句完整可读。
            两次误判记在这里:①「seek 后 currentTime 一直是 0」是因为 Python 的
            http.server 不支持 Range 请求,本地没法 seek,不是视频的问题;
            ②「右边被切掉」是因为截图抓在 3.21s,揭开动画才走到 82%,右边 18% 本来
            就还没揭开 —— 把「正在揭开」看成了「被切掉」
- [x] **2. 视频位置微调** — 以用户视角看哪个位置最舒服,和其他模块的位置要协调
- [x] **3. 按截图做活动方的界面** — `ac67174`
      - [x] 问候横幅、四个数字、活动卡片(带填充条)、两步创建流程,都在
      - [x] 创建时补上「地点」——主办方卡片一直在标题下面印地点,而创建流程从来不收
      - [x] 简介编辑器补上「标签」;活动页搜索把地点和标签一起搜进去
      - [x] **修:describe 少传一个参数**,所有简介写入在发出去之前就失败(见 `ac67174`)
      - [x] 「先在左边选一场活动」代替永远转不完的骨架屏;「还没创建活动」和
            「正在加载活动」不再同时挂着
      - [x] 问候语按时间分早/下午/晚上(原来任何时候都说「早上好」)
- [x] **4. 按截图做个人资料的界面** — `6d975e2`
      - [x] 资料能填、能存、能读回来:名字、简介、城市、X、GitHub、网站
      - [x] **在本机 anvil 上真的跑通一遍**:登录 → 部署目录合约 → 填 → 存 → 回概览,
            名字取代地址出现在横幅上
      - [x] **修:部署按钮只有浏览器插件用户能按**(邮箱/passkey 路径给的是一句英文报错)
      - [x] **修:部署 gas 写死 1,400,000,实际要 1,689,094**——按一次就是花钱买一次失败
      - [x] **修:setProfile 的 gas 公式漏了「每个字段一个长度槽」**,填满资料必存不上
      - [x] `/me` 签出状态没有登录入口(却让人去别的页面登录)
- [x] **5. 手机版界面同样要做** — `2aa46dc`
      - [x] 账户页的分区导航:250px 竖排 → 56px 横滑胶囊
      - [x] 八条路线在 390×844 重测:零横向溢出、零 44px 以下目标、零 13px 以下正文
      - [x] 会场页「清除这个密钥」21px→44px(门口单手按、按下去密钥就没了的按钮)
- [x] **6. 扫码的界面做好** — `/floor`
      - [x] 本机跑通:报名 → 门口签到 → 三个同伴作证 → 判定到场
      - [x] 摄像头界面是全屏覆盖层,`object-contain` 不裁画面(之前的 fullPage 截图
            看着像嵌在页面里,那是截图方式的问题,不是页面的)
- [x] **7. 展示自己二维码的界面做好** — `/venue`,1920×1080 和 390×844 都核过
- [x] **8. 自检:页面的样子是不是和设计图一样** — `234d896` / `3b0eade`
      - [x] **先把设计稿找回来**:用户当时是贴图发的,磁盘上没有。从会话记录的
            base64 里取回 32 张,挑出三张真稿存进 `docs/设计稿/`
      - [x] 逐块核完,补上:活动发现的**分类胶囊**(第二排)、个人主页 banner 上的
            **编辑资料**按钮、**圆头像**
      - [x] 明确不做的和做法不同的,都写在 `docs/设计稿/README.md` 里
- [x] **9. 自检:从用户角度看,每个模块是不是都在合适的位置**
      - [x] 主办方:「先在左边选一场活动」代替永远转不完的骨架屏;两个互相矛盾的
            空状态不再同时挂着;问候语按时间分早/下午/晚上
      - [x] 个人主页:名字取代地址、简介和链接读得出来、编辑入口挪到 banner
      - [x] 公开记录:活动还开着的时候不再说别人「押金没收」
- [x] **10. 测试每一个按键是否都能用** — `f5a6f31`
      - [x] 八条路线逐个按。修掉:一条 `href=""` 的死链、一个在非安全来源上什么都
            不做还抛异常的复制按钮
      - [x] 检测器本身也修了 —— 它按「正文长度变没变」判断,空列表上的筛选胶囊被
            误报成死按钮。现在把选中态算进签名
      - [x] 剩下两条「无反应」是按已经选中的那一项,本来就不该有反应
- [x] **11. 全方位可用性测试** —— 浏览器这一侧做完了,线上实测
      - [x] 主链路在本机 anvil 上端到端跑通:报名 → 门口签到 → 三个同伴作证 →
            判定到场 → 结算 → 公开记录;以及部署目录合约 → 填资料 → 存 → 读回来
      - [x] **线上**按钮扫描八条路线全过(`scripts/button-sweep.mjs`)
      - [x] **线上**手机体检八条路线全过(`scripts/mobile-sweep.mjs`)
      - [x] **线上**首页视频确认在播(currentTime 2.97 → 1.72,循环回绕)
      - [x] 修:**登录弹窗按过一次之后,每开一个页面都会自己弹**
            (`scripts/signin-dialog-probe.mjs`,线上四条全过)
      - [x] 修:样例活动上的报名按钮是条死路
      - [x] 修:未登录时手机上四处 12px,其中一处是「要不要把身份交出去」那段说明
      - [ ] **还差:三台真机扫码**。这个我做不了 —— 要三部手机和三个人
- [ ] **12. 部署合约** — **卡在这里,需要用户**
      - 押金合约 `0xEa057c5a6F431eFF29F573E4db4bc49cD52Ee679` 在 testnet 上**已经部署**
      - 目录合约(存标题/地点/标签/个人资料)**还没有**,地址会是
        `0x95Ae04c2F8A97b0db61A9C1df16F73f2B280Ca8f`
      - 现在线上按「部署目录合约」**真的能成了**(之前两个坑:只认浏览器插件、
        gas 写死 1,400,000 不够用,都已修)
      - 我做不了的是**签名和付 gas**:约 1.69M gas,按 102 gwei 算 ≈ 0.18 MON。
        需要用户在 https://zhxinyue29.github.io/peerproof/me/ 登录后按那个按钮

**不要随便停止。** 清单没清空就继续下一项,不要做完一件就回来汇报。

**卡住就跳过,不要停。** 某一步实在搞不定,在这里写明卡在哪、试过什么,然后做下一项。

---

## 每个页面的验收要求(六条,缺一不算做完)

做完 3、4、9、10、11 里的任何一个页面,都要走完这六条再打勾:

- **a. 对着设计稿逐块核** —— 有的做了;没做的写明为什么不做
- **b. 从用户视角看每个模块的位置** —— 不是"有没有",是"在这儿舒不舒服"
- **c. 点每一个按钮** —— 确认都能用,不是看着能用
- **d. 手机版同样过一遍**
- **e. 截图存档**
- **f. 推上线,并核实线上真的生效** —— 不是推完就算,要去线上抓一次

---

## 用户定过的约定(不是任务,是边界)

- 社区声誉值、活动满意度 —— **不做**,这个系统永远不会产生这两个数
- 主办方奖金池 —— **不做**,与「主办方从头到尾碰不到一分钱」冲突
- 主办方押金(取消则扣) —— 做成**功能说明**,不写进合约
- USD 折算、较上月 —— **留着**,将来会有;现在显示 `—` 而不是 0
- 我的身份 —— 用户说去掉
- 活动标签、个人资料 —— **存链上**,不存浏览器本地(只有自己看得见等于没有)

## 我做不了、需要用户的

- ~~登录态的所有页面截不到~~ —— **解决了**。`?dev=1` 会给出一个一次性本地密钥,
  用它就能截到 `/me`、`/organizer` 登录后的样子;本机 anvil 上还能给它打款,
  于是整条链上流程都能自动跑。线上也能用(只是那个地址没有 MON)
- 三台真机扫码测试
- 任何要签名的链上写入

---

## 这一轮留下的三件工具(以后别再从头写)

| 脚本 | 干什么 | 只能线上跑? |
|---|---|---|
| `scripts/button-sweep.mjs` | 按遍每个按钮,报告没反应的和空 href | 否,但线上更全 |
| `scripts/mobile-sweep.mjs` | 390×844 量溢出 / 44px / 13px | 否,但**未登录**的那几段只有线上才走到 |
| `scripts/signin-dialog-probe.mjs` | 登录弹窗该弹时弹、关了不回来 | **是**。Privy 只认线上域名 |

另外:`?dev=1` + 本机 anvil(`scripts/dev-chain.sh`)= 整条链上流程可以自动跑完,
包括登录态的截图。以前以为做不到,做得到。
