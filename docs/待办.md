# 待办

**开工第一件事:读这个文件。做完一项就把 `[ ]` 改成 `[x]` 并写上提交号。**

这份清单存在的理由:之前"还剩什么"只活在聊天消息里,一旦停下或换会话就丢了,
用户得把需求重讲一遍。写进仓库之后,任何一次中断都是可恢复的。

规则:
- 不要因为"干完一件"就停下来汇报。清单没清空就继续下一项。
- 真的要停(上下文用尽、或需要用户决定),**先把这个文件更新到最新**再停。
- 新需求进来就加到这里,不要只记在脑子里。

---

## 进行中

写成**目标**而不是碎片。之前只列了「四步流程」「数据分析页」这种子任务,做完一个就
以为推进了,而「这一页和设计稿一样了吗、上线了吗」没人负责 —— 用户原话是「把活动方
和个人资料的页面设计都做好上线」,那是两个目标,不是六个碎片。

每个目标的验收条件都一样,缺一不算完成:

  a. 对着设计稿逐块核 —— 有的做了、没有的写明为什么不做
  b. 从用户视角看每个模块的位置是否合适
  c. 点每一个按钮,确认都能用
  d. 手机版同样过一遍
  e. 截图存档
  f. 推上线并核实线上真的生效

---

### 目标一:活动方(组织方)页面做好并上线

- [x] 控制台问候横幅(代码) — `a96b65b`
- [ ] 问候横幅验收 — 未登录时它在 `IdentityGate` 后面,我截不到,需要真会话
- [ ] 创建活动四步流程 — 现在是两步。四步映射到真实参数:
      基础信息(标题/简介/地点/标签) → 验证规则(k、最低成行、现场报名)
      → 押金与时间窗 → 预览发布
      **不做主办方奖金池**(与"主办方碰不到钱"冲突,用户已同意)
      主办方押金(取消则扣)做成**功能说明**,不写进合约
- [ ] 数据分析页 — 参与人数趋势折线图、结算进度环形图。没数据就是空坐标轴
- [ ] 活动管理区 — 设计稿 05 那块(报名数/已验证/待验证/未通过)
- [ ] a–f 全部走一遍

### 目标二:个人资料 / 个人主页做好并上线

- [x] `/me` 按新稿重做(四数字块/左侧栏/两个列表/影响力行) — `278bce2`
- [x] 资料编辑表单(昵称/简介/城市/X/GitHub/网站) — `f8557ae`
- [ ] 资料**展示** — 填完之后要显示在主页上,现在只有编辑表单,没有展示
- [ ] 登录态验收 — 无头浏览器没有会话,登录后的样子我一张都截不到
- [ ] a–f 全部走一遍

### 跨两个目标的

- [ ] `/events` 分类 chips 接上 `tags` — 合约字段已有,前端筛选没接
- [ ] 手机版界面 — 用户最早那张手机稿(汉堡菜单、三步条、底部五个 tab)一直没做
- [ ] 扫码 / 二维码两页对稿 — `/floor` 和 `/venue`,只确认过能打开
- [ ] 全面可用性测试 — 系统地跑一遍,而不是零散地做
- [ ] 2:30 demo 视频 — 用户录,我抽帧定剪辑点、ffmpeg 剪

## 需要用户做的(我做不了)

- [ ] **部署 `EventDirectory`** — 约 0.14 MON,要用户签名。部署后 `tags` 和 `Profile` 才真的能用。
      两条路,任选:
      1. 去 `/organizer`,目录不存在时那里会出现一个部署按钮(2026-09-20 之前它被
         `devMode` 挡着,线上看不见,这就是这件事一直卡住的原因)
      2. 直接建第一个活动 —— 只要填了标题/简介/链接,创建流程会**自动先部署**它
         (`app/organizer/page.tsx:562`)
- [ ] **建第一个真活动** — 要用户签名。建完 `/event`、`/verify`、`/me` 三页才有内容可验。
- [ ] **三台真机扫码测试** — 唯一没验过的核心动作。截止 2026-10-13。

## 已完成

- [x] 首屏循环视频 + 字卡落款 — `674e8c1`
- [x] 合约加 `tags` 和 `Profile`,68 测试全过 — `a96b65b`
- [x] 组织方问候横幅(代码) — `a96b65b`
- [x] `/me` 按新稿重做(四数字块/左侧栏/两个列表/影响力行) — `278bce2`
- [x] 登录按钮修复(原本是个假链接)、钱包登录恢复、弹窗关不掉 — `0b4b6bd`
- [x] 参与方五页统一顶栏、`/events` 按设计稿重排、样例详情页、四步弹窗 — `b43c3c1` `3f2e3e4` `ce8780d`

---

## 不做,以及为什么

写在这里是为了防止下一轮"顺手补上":

- **社区声誉值 / 活动满意度** — 这个系统永远不会产生这两个数(没有评分入口)。
- **主办方奖金池** — 与"主办方从头到尾碰不到一分钱"直接冲突,那是整个产品的立身之本。
- **通知铃铛** — 没有通知系统,挂个铃铛点了没反应。
- **四种验证方式** — 实际只有两种:信标签到 + 同伴互证。地理位置可伪造,信标存在的
  理由正是替代它。
- **活动封面图上传** — 没有图床。现在按活动 id 生成图案。
- **USD 折算 / 较上月** — 这两个**要留着**,将来会有(接价格源、攒够历史),
  现在显示 `—` 而不是 0。`—` 是"还不知道",0 是一个断言。
