# 当前状态

最后更新 2026-09-18。这份文件记录**现在是什么样**，不是待办清单。

## 线上

<https://zhxinyue29.github.io/peerproof/> —— HTTPS，手机直接开，摄像头可用，不需要装任何东西。

| | 地址 |
|---|---|
| escrow（保管押金） | `0xEa057c5a6F431eFF29F573E4db4bc49cD52Ee679` · 区块 63497445 |
| EventDirectory（存标题） | `0x223Cd221a51242b48D1b42985991665400b2a28F` · **尚未部署** |
| 部署/主办账户 | `0x4cb6B0E49Fd04DBAC6671D0d7e21ba2b46f1DF7D` |

Directory 没部署的影响**只有显示**：活动会显示成「活动 #1」而不是标题，建活动时会提示一句「描述没存上」。押金、签到、作证、结算全部照常——`describe` 是独立的第二笔交易，代码里包了 try/catch。

## 已验证

| 项 | 结果 |
|---|---|
| 合约单元测试 | 68/68 |
| 生产构建 | 通过，11 页静态导出 |
| 七个页面 × 桌面/手机 | 无报错、无横向溢出、无未翻译 key |
| 中英双语 | 393+ 词条，字典类型强制同步，`scripts/i18n-audit.mjs` 零缺失 |
| 扫人（假摄像头跑真实页面） | `VOUCH LANDED ON CHAIN after 1.1s` |
| 扫场地码签到 | `CHECK-IN ON CHAIN after 1.0s` |
| 切链守卫 | 四种情况全过，含「钱包谎称已切换」 |

## 还没做

- **三设备真机扫码** —— 管线已用真摄像头验证，真手机 + 真人没跑过。这是唯一还没验的核心动作。
- **EventDirectory 部署** —— 一次 0.14 MON，只影响标题显示
- **2:30 demo 视频** —— 你录，我可以抽帧定剪辑点、用 ffmpeg 剪
- **`cre login` / `cre simulate`** —— 要你的 Chainlink 账号
- **Envio GraphQL 端点** —— 卡在免费版拿不到 token
- **建活动第三步「Review」** —— 设计稿有，现在两步

## 可重复的验证命令

```bash
cd contracts && forge test                      # 68/68
cd web && npx tsc --noEmit && npm run build     # 0 错误，11 页
node scripts/i18n-audit.mjs                     # 有原始 key 漏出就报错

# 下面三个需要先起 anvil + scripts/dev-chain.sh + npm run dev
node scripts/scan-e2e.mjs                       # 扫人
node scripts/venue-e2e.mjs                      # 签到 + 扫人
node scripts/chain-guard-test.mjs               # 切链守卫（不需要链）
```

## 截图

`docs/screens-v3/` —— 七个页面 × 桌面/手机 × 中英，28 张，对应当前线上界面。

`docs/screens/` 那套是**改版前**的，`产品说明_给设计师.md` 仍按编号引用它们，所以暂时保留。

## 已知的、花掉但没产生价值的钱

约 0.43 MON，三次，全部是流程和界面问题：

1. 昨晚部的 escrow `0x6Dcaa…` —— 我在你部完之后又改了合约
2. 配在那个作废 escrow 上的 directory —— 我让你在配置还没换过去时就点了按钮
3. 多余的 escrow `0x4a5e…` —— 两个几乎一样的橙色框放在同一页

第三次之后，escrow 部署按钮已经删除（`0637947`），`?dev=1` 下只剩 directory 一个框。
