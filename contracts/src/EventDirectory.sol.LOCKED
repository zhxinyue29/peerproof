# 不要再改 contracts/src/EventDirectory.sol

这个合约的地址是 CREATE2 从**字节码**算出来的。改任何东西——包括一条注释——都会
改变 solc 附在末尾的 metadata 哈希,字节码就变了,地址就变了,而**已经部署的那一份
和写进去的所有标题、封面、个人资料,全都会变成前端再也不去看的地址上的孤儿数据**。

2026-09-20 已经发生过一次:一位主办方建好活动、部署了目录、写进了标题「测试1」,
之后我把 MAX_COVER 从 300 改成 12000(为了支持封面上传),地址从
`0xc1Ca4e0a40585d03A4c4aAe301CAD9880FB49aD9` 变成了别的,标题当场"消失"。
恢复的办法是把文件逐字节还原成 `88b6034` 那一版——注释也不能差一个字。

## testnet 上已经部署的

- 押金合约 `0xEa057c5a6F431eFF29F573E4db4bc49cD52Ee679`
- 目录合约 `0xc1Ca4e0a40585d03A4c4aAe301CAD9880FB49aD9`(对应 `88b6034` 的源码)

## 要加功能怎么办

加一个**新的、独立的合约**。封面上传就该这么做:covers 合约自己部署、自己演进,
目录合约一个字节都不动。
